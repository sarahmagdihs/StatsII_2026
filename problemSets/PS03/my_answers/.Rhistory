stargazer(poisson_reg)
##########################
# Title:        Problem Set 3
# Description:  Applied Stats II
# Author:       Sarah Magdihs
# R version:    R 4.5.1
#Last modified: 23.03.2026
###########################
#####################
#### Set Up:
# load libraries
# set wd
# clear global .envir
#####################
# remove objects
rm(list=ls())
# detach all libraries
detachAllPackages <- function() {
basic.packages <- c("package:stats", "package:graphics", "package:grDevices", "package:utils", "package:datasets", "package:methods", "package:base")
package.list <- search()[ifelse(unlist(gregexpr("package:", search()))==1, TRUE, FALSE)]
package.list <- setdiff(package.list, basic.packages)
if (length(package.list)>0)  for (package in package.list) detach(package,  character.only=TRUE)
}
detachAllPackages()
# load libraries
pkgTest <- function(pkg){
new.pkg <- pkg[!(pkg %in% installed.packages()[,  "Package"])]
if (length(new.pkg))
install.packages(new.pkg,  dependencies = TRUE)
sapply(pkg,  require,  character.only = TRUE)
}
# here is where you load any necessary packages
# ex: stringr
# lapply(c("stringr"),  pkgTest)
lapply(c("nnet", "MASS", "stargazer", "ggplot2"),  pkgTest)
# set wd for current folder
setwd(dirname(rstudioapi::getActiveDocumentContext()$path))
##########################
# Title:        Problem Set 3
# Description:  Applied Stats II
# Author:       Sarah Magdihs
# R version:    R 4.5.1
#Last modified: 23.03.2026
###########################
#####################
#### Set Up:
# load libraries
# set wd
# clear global .envir
#####################
# remove objects
rm(list=ls())
# detach all libraries
detachAllPackages <- function() {
basic.packages <- c("package:stats", "package:graphics", "package:grDevices", "package:utils", "package:datasets", "package:methods", "package:base")
package.list <- search()[ifelse(unlist(gregexpr("package:", search()))==1, TRUE, FALSE)]
package.list <- setdiff(package.list, basic.packages)
if (length(package.list)>0)  for (package in package.list) detach(package,  character.only=TRUE)
}
detachAllPackages()
# load libraries
pkgTest <- function(pkg){
new.pkg <- pkg[!(pkg %in% installed.packages()[,  "Package"])]
if (length(new.pkg))
install.packages(new.pkg,  dependencies = TRUE)
sapply(pkg,  require,  character.only = TRUE)
}
# here is where you load any necessary packages
# ex: stringr
# lapply(c("stringr"),  pkgTest)
lapply(c("nnet", "MASS", "stargazer", "ggplot2"),  pkgTest)
# set wd for current folder
setwd(dirname(rstudioapi::getActiveDocumentContext()$path))
getwd()
#####################
# Problem 1
#####################
# load data
gdp_data <- read.csv("https://raw.githubusercontent.com/ASDS-TCD/StatsII_2026/main/datasets/gdpChange.csv", stringsAsFactors = F)
head(gdp_data)
str(gdp_data)
#Response: GDPWdiff
#Step 1: Get variables ready
#Recode GDPWdiff
gdp_data$GDPWdiff_categ <- ifelse(gdp_data$GDPWdiff > 0, "positive",
ifelse(gdp_data$GDPWdiff < 0, "negative", "no change"))
gdp_data$GDPWdiff_categ <- factor(gdp_data$GDPWdiff_categ,
levels = c("no change", "negative", "positive"))
#no change as first level because then it is treated as the reference category
#Explanatory: REG, OIL
#cpnvert to factor
gdp_data$REG_f <- factor(gdp_data$REG, levels = c(0, 1),
labels = c("Non-Democracy", "Democracy"))
gdp_data$OIL_f <- factor(gdp_data$OIL, levels = c(0, 1),
labels = c("Fuel exports below 50%", "Fuel exports over 50%"))
#converting them to factors is not really necessary.
#This is mainly for ease concerning interpretation at a later stage
str(gdp_data)
# unordered multinomial logit
#GDPWdiff as the output and ”no change” as the reference category
multinom_unordered <- multinom(GDPWdiff_categ ~ REG_f + OIL_f, data = gdp_data)
summary(multinom_unordered)
#exponentiate coefs for odds
odds_unordered <- exp(coef(multinom_unordered))
# get p values
z <- summary(multinom_unordered)$coefficients/summary(multinom_unordered)$standard.errors
(p <- (1 - pnorm(abs(z), 0, 1)) * 2)
####save outputs here.
stargazer(multinom_unordered)
#ordered
# Need ordered factor: negative --> no change --> positive
gdp_data$GDPWdiff_ordered <- ordered(gdp_data$GDPWdiff_categ,
levels = c("negative", "no change", "positive"))
multinom_ordered <- polr(GDPWdiff_ordered ~ REG_f + OIL_f, data = gdp_data, Hess = TRUE)
summary(multinom_ordered)
# Calculate a p value
ctable <- coef(summary(multinom_ordered))
p_ordered <- pnorm(abs(ctable[, "t value"]), lower.tail = FALSE) * 2
cbind(ctable, "p value" = round(p_ordered, 3))
# Calculate confidence intervals
ci_ordered <- confint(multinom_ordered)
# convert to odds ratio
exp(cbind(OR = coef(multinom_ordered), ci_ordered))
####save outputs here.
stargazer(multinom_ordered)
#####################
# Problem 2
#####################
# load data
mexico_elections <- read.csv("https://raw.githubusercontent.com/ASDS-TCD/StatsII_2026/main/datasets/MexicoMuniData.csv")
#check out the data
str(mexico_elections)
summary(mexico_elections$PAN.visits.06)
table(mexico_elections$PAN.visits.06)
var(mexico_elections$PAN.visits.06)
#poisson
poisson_reg <- glm(PAN.visits.06 ~ competitive.district + marginality.06 + PAN.governor.06,
data = mexico_elections,
family = poisson(link = "log"))
summary(poisson_reg)
stargazer(poisson_reg)
#p and z for competitive/swing districts
coef_swing <- summary(poisson_reg)$coefficients["competitive.district", ]
coef_swing["z value"]
coef_swing["Pr(>|z|)"]
#exponentiate coefs for interpretation
poisson_expcoef <- exp(coef(poisson_reg))
#specific case: estimated mean number of visits from the winning PAN presidential candidate
#for a hypothetical district that was competitive (competitive.district=1), had
#an average poverty level (marginality.06 = 0), and a PAN governor (PAN.governor.06=1)
predict_case <- data.frame(
competitive.district = 1,
marginality.06 = 0,
PAN.governor.06 = 1
)
predict(poisson_reg, newdata = predict_case, type = "response")
cbind(ctable, "p value" = round(p_ordered, 3))
str(gdp_data)
stargazer(multinom_unordered)
z
p
gdp_data$GDPWdiff_categ
summary(gdp_data$GDPWdiff_categ)
odds_unordered
stargazer(odds_unordered,
summary = FALSE,
title = "OR: Unordered Multinomial Logit")
cbind(ctable, "p value" = round(p_ordered, 3))
exp(cbind(OR = coef(multinom_ordered), ci_ordered))
odds_ordered <- exp(cbind(OR = coef(multinom_ordered), ci_ordered))
odds_ordered
stargazer(odds_ordered)
stargazer(multinom_ordered)
stargazer(odds_ordered)
stargazer(
multinom_ordered,
single.row = FALSE
)
# coefficient table with p-values
ologit_ct <- coef(summary(ologit_mod))
multi_ord_table <- coef(summary(multinom_ordered))
orderedreg_p  <- 2 * (1 - pnorm(abs(multi_ord_table[, "t value"])))
multi_ord_results <- round(cbind(
multi_ord_table,
p.value = orderedreg_p
), 4)
multi_ord_results
multi_ord_table <- coef(summary(multinom_ordered))
multi_ord_results <- round(cbind(
multi_ord_table
), 4)
multi_ord_results
multi_ord_table <- coef(summary(multinom_ordered$zeta))
multi_ord_table <- coef(summary(multinom_ordered[, "zeta"]))
summary_ordered_reg <- summary(multinom_ordered)
#thresholds stored in zeta
cutoffs_ordered <- summary_ordered_reg$zeta
se_cutoffs_ordered <- summary_ordered$standard.errors[names(cutoffs)]
summary_ordered_reg <- summary(multinom_ordered)
#thresholds stored in zeta
cutoffs_ordered <- summary_ordered_reg$zeta
se_cutoffs_ordered <- summary_ordered_reg$standard.errors[names(cutoffs)]
#cutoff points
summary_ordered_reg <- summary(multinom_ordered)
#thresholds stored in zeta
cutoffs_ordered <- summary_ordered_reg$zeta
se_cutoffs_ordered <- summary_ordered_reg$standard.errors[names(cutoffs)]
summary_ordered_reg <- summary(multinom_ordered)
#thresholds stored in zeta
cutoffs_ordered <- summary_ordered_reg$zeta
se_cutoffs_ordered <- summary_ordered_reg$standard.errors[names(cutoffs_ordered)]
cutoffs_table <- data.frame(
Threshold = names(cutoffs_ordered),
Estimate = cutoffs_ordered,
Std.Error = se_cutoffs_ordered
)
cutoffs_table
table_with_cutoffs <- coef(summary(multinom_ordered))
table_with_cutoffs
ologit_results <- round(cbind(
table_with_cutoffs,
), 4)
ologit_results <- round(cbind(
table_with_cutoffs
), 4)
ologit_results
summary_ordered_reg <- summary(multinom_ordered)
#thresholds stored in zeta
cutoffs_ordered <- summary_ordered_reg$zeta
cutoffs_ordered
cutoffs_table <- data.frame(
Threshold = names(cutoffs_ordered),
Estimate = cutoffs_ordered
)
cutoffs_table <- round(cutoffs_table, 3)
stargazer(cutoffs_ordered)
##########################
# Title:        Problem Set 3
# Description:  Applied Stats II
# Author:       Sarah Magdihs
# R version:    R 4.5.1
#Last modified: 23.03.2026
###########################
#####################
#### Set Up:
# load libraries
# set wd
# clear global .envir
#####################
# remove objects
rm(list=ls())
# detach all libraries
detachAllPackages <- function() {
basic.packages <- c("package:stats", "package:graphics", "package:grDevices", "package:utils", "package:datasets", "package:methods", "package:base")
package.list <- search()[ifelse(unlist(gregexpr("package:", search()))==1, TRUE, FALSE)]
package.list <- setdiff(package.list, basic.packages)
if (length(package.list)>0)  for (package in package.list) detach(package,  character.only=TRUE)
}
detachAllPackages()
# load libraries
pkgTest <- function(pkg){
new.pkg <- pkg[!(pkg %in% installed.packages()[,  "Package"])]
if (length(new.pkg))
install.packages(new.pkg,  dependencies = TRUE)
sapply(pkg,  require,  character.only = TRUE)
}
# here is where you load any necessary packages
# ex: stringr
# lapply(c("stringr"),  pkgTest)
lapply(c("nnet", "MASS", "stargazer", "ggplot2"),  pkgTest)
# set wd for current folder
setwd(dirname(rstudioapi::getActiveDocumentContext()$path))
getwd()
#####################
# Problem 1
#####################
# load data
gdp_data <- read.csv("https://raw.githubusercontent.com/ASDS-TCD/StatsII_2026/main/datasets/gdpChange.csv", stringsAsFactors = F)
head(gdp_data)
str(gdp_data)
#Recoding of Variables
#Response: GDPWdiff
gdp_data$GDPWdiff_categ <- ifelse(gdp_data$GDPWdiff > 0, "positive",
ifelse(gdp_data$GDPWdiff < 0, "negative", "no change"))
gdp_data$GDPWdiff_categ <- factor(gdp_data$GDPWdiff_categ,
levels = c("no change", "negative", "positive"))
#no change as first level because then it is treated as the reference category
summary(gdp_data$GDPWdiff_categ)
#Explanatory: REG, OIL
#cpnvert to factor
gdp_data$REG_f <- factor(gdp_data$REG, levels = c(0, 1),
labels = c("Non-Democracy", "Democracy"))
gdp_data$OIL_f <- factor(gdp_data$OIL, levels = c(0, 1),
labels = c("Fuel exports below 50%", "Fuel exports over 50%"))
#converting them to factors is not really necessary.
#This is mainly for ease concerning interpretation at a later stage/as a reminder for me
str(gdp_data)
# TASK 1: unordered multinomial logit
#GDPWdiff as the output and ”no change” as the reference category
multinom_unordered <- multinom(GDPWdiff_categ ~ REG_f + OIL_f, data = gdp_data)
summary(multinom_unordered)
#exponentiate coefs for odds/interpretation
odds_unordered <- exp(coef(multinom_unordered))
# p values
z <- summary(multinom_unordered)$coefficients/summary(multinom_unordered)$standard.errors
(p <- (1 - pnorm(abs(z), 0, 1)) * 2)
####save output
stargazer(multinom_unordered)
stargazer(odds_unordered,
summary = FALSE,
title = "OR: Unordered Multinomial Logit")
#TASK 2: ordered multinomial logit
# Need ordered factor: negative --> no change --> positive
gdp_data$GDPWdiff_ordered <- ordered(gdp_data$GDPWdiff_categ,
levels = c("negative", "no change", "positive"))
multinom_ordered <- polr(GDPWdiff_ordered ~ REG_f + OIL_f, data = gdp_data, Hess = TRUE)
summary(multinom_ordered)
#p value
ctable <- coef(summary(multinom_ordered))
p_ordered <- pnorm(abs(ctable[, "t value"]), lower.tail = FALSE) * 2
cbind(ctable, "p value" = round(p_ordered, 3))
#cutoff points
summary_ordered_reg <- summary(multinom_ordered)
#extrcact thresholds stored in zeta
cutoffs_ordered <- summary_ordered_reg$zeta
#confidence intervals
ci_ordered <- confint(multinom_ordered)
# convert to odds ratio
odds_ordered <- exp(cbind(OR = coef(multinom_ordered), ci_ordered))
####save output
stargazer(multinom_ordered)
#cutoffs
stargazer(cutoffs_ordered)
#odds
stargazer(odds_ordered)
#####################
# Problem 2
#####################
# load data
mexico_elections <- read.csv("https://raw.githubusercontent.com/ASDS-TCD/StatsII_2026/main/datasets/MexicoMuniData.csv")
#check out the data
str(mexico_elections)
summary(mexico_elections$PAN.visits.06)
table(mexico_elections$PAN.visits.06)
var(mexico_elections$PAN.visits.06)
#TASK 1: Poisson regression
poisson_reg <- glm(PAN.visits.06 ~ competitive.district + marginality.06 + PAN.governor.06,
data = mexico_elections,
family = poisson(link = "log"))
summary(poisson_reg)
stargazer(poisson_reg)
#p and z for competitive/swing districts
coef_swing <- summary(poisson_reg)$coefficients["competitive.district", ]
coef_swing["z value"]
coef_swing["Pr(>|z|)"]
#exponentiate coefs for interpretation (TASK 2)
poisson_expcoef <- exp(coef(poisson_reg))
#TASK 3:
#specific case: estimated mean number of visits from the winning PAN presidential candidate
#for a hypothetical district that was competitive (competitive.district=1), had
#an average poverty level (marginality.06 = 0), and a PAN governor (PAN.governor.06=1)
predict_case <- data.frame(
competitive.district = 1,
marginality.06 = 0,
PAN.governor.06 = 1
)
predict(poisson_reg, newdata = predict_case, type = "response")
coef_swing["z value"]
coef_swing["Pr(>|z|)"]
stargazer(poisson_reg)
coef_swing
stargazer(coef_swing)
poisson_expcoef
stargazer(poisson_expcoef)
predict(poisson_reg, newdata = predict_case, type = "response")
predicted <- predict(poisson_reg, newdata = predict_case, type = "response")
predicted
stargazer(predicted)
predict(poisson_reg, newdata = predict_case, type = "response")
##########################
# Title:        Problem Set 3
# Description:  Applied Stats II
# Author:       Sarah Magdihs
# R version:    R 4.5.1
#Last modified: 23.03.2026
###########################
#####################
#### Set Up:
# load libraries
# set wd
# clear global .envir
#####################
# remove objects
rm(list=ls())
# detach all libraries
detachAllPackages <- function() {
basic.packages <- c("package:stats", "package:graphics", "package:grDevices", "package:utils", "package:datasets", "package:methods", "package:base")
package.list <- search()[ifelse(unlist(gregexpr("package:", search()))==1, TRUE, FALSE)]
package.list <- setdiff(package.list, basic.packages)
if (length(package.list)>0)  for (package in package.list) detach(package,  character.only=TRUE)
}
detachAllPackages()
# load libraries
pkgTest <- function(pkg){
new.pkg <- pkg[!(pkg %in% installed.packages()[,  "Package"])]
if (length(new.pkg))
install.packages(new.pkg,  dependencies = TRUE)
sapply(pkg,  require,  character.only = TRUE)
}
# here is where you load any necessary packages
# ex: stringr
# lapply(c("stringr"),  pkgTest)
lapply(c("nnet", "MASS", "stargazer", "ggplot2"),  pkgTest)
# set wd for current folder
setwd(dirname(rstudioapi::getActiveDocumentContext()$path))
getwd()
#####################
# Problem 1
#####################
# load data
gdp_data <- read.csv("https://raw.githubusercontent.com/ASDS-TCD/StatsII_2026/main/datasets/gdpChange.csv", stringsAsFactors = F)
head(gdp_data)
str(gdp_data)
#Recoding of Variables
#Response: GDPWdiff
gdp_data$GDPWdiff_categ <- ifelse(gdp_data$GDPWdiff > 0, "positive",
ifelse(gdp_data$GDPWdiff < 0, "negative", "no change"))
gdp_data$GDPWdiff_categ <- factor(gdp_data$GDPWdiff_categ,
levels = c("no change", "negative", "positive"))
#no change as first level because then it is treated as the reference category
summary(gdp_data$GDPWdiff_categ)
#Explanatory: REG, OIL
#cpnvert to factor
gdp_data$REG_f <- factor(gdp_data$REG, levels = c(0, 1),
labels = c("Non-Democracy", "Democracy"))
gdp_data$OIL_f <- factor(gdp_data$OIL, levels = c(0, 1),
labels = c("Fuel exports below 50%", "Fuel exports over 50%"))
#converting them to factors is not really necessary.
#This is mainly for ease concerning interpretation at a later stage/as a reminder for me
str(gdp_data)
# TASK 1: unordered multinomial logit
#GDPWdiff as the output and ”no change” as the reference category
multinom_unordered <- multinom(GDPWdiff_categ ~ REG_f + OIL_f, data = gdp_data)
summary(multinom_unordered)
#exponentiate coefs for odds/interpretation
odds_unordered <- exp(coef(multinom_unordered))
# p values
z <- summary(multinom_unordered)$coefficients/summary(multinom_unordered)$standard.errors
(p <- (1 - pnorm(abs(z), 0, 1)) * 2)
####save output
stargazer(multinom_unordered)
stargazer(odds_unordered,
summary = FALSE,
title = "OR: Unordered Multinomial Logit")
#TASK 2: ordered multinomial logit
# Need ordered factor: negative --> no change --> positive
gdp_data$GDPWdiff_ordered <- ordered(gdp_data$GDPWdiff_categ,
levels = c("negative", "no change", "positive"))
multinom_ordered <- polr(GDPWdiff_ordered ~ REG_f + OIL_f, data = gdp_data, Hess = TRUE)
summary(multinom_ordered)
#p value
ctable <- coef(summary(multinom_ordered))
p_ordered <- pnorm(abs(ctable[, "t value"]), lower.tail = FALSE) * 2
cbind(ctable, "p value" = round(p_ordered, 3))
#cutoff points
summary_ordered_reg <- summary(multinom_ordered)
#extract thresholds stored in zeta
cutoffs_ordered <- summary_ordered_reg$zeta
#confidence intervals
ci_ordered <- confint(multinom_ordered)
# convert to odds ratio
odds_ordered <- exp(cbind(OR = coef(multinom_ordered), ci_ordered))
####save output
stargazer(multinom_ordered)
#cutoffs
stargazer(cutoffs_ordered)
#odds
stargazer(odds_ordered)
#####################
# Problem 2
#####################
# load data
mexico_elections <- read.csv("https://raw.githubusercontent.com/ASDS-TCD/StatsII_2026/main/datasets/MexicoMuniData.csv")
#check out the data
str(mexico_elections)
summary(mexico_elections$PAN.visits.06)
table(mexico_elections$PAN.visits.06)
var(mexico_elections$PAN.visits.06)
#TASK 1: Poisson regression
poisson_reg <- glm(PAN.visits.06 ~ competitive.district + marginality.06 + PAN.governor.06,
data = mexico_elections,
family = poisson(link = "log"))
summary(poisson_reg)
stargazer(poisson_reg)
#p and z for competitive/swing districts
coef_swing <- summary(poisson_reg)$coefficients["competitive.district", ]
coef_swing["z value"]
coef_swing["Pr(>|z|)"]
stargazer(coef_swing)
#exponentiate coefs for interpretation (TASK 2)
poisson_expcoef <- exp(coef(poisson_reg))
stargazer(poisson_expcoef)
#TASK 3:
#specific case: estimated mean number of visits from the winning PAN presidential candidate
#for a hypothetical district that was competitive (competitive.district=1), had
#an average poverty level (marginality.06 = 0), and a PAN governor (PAN.governor.06=1)
predict_case <- data.frame(
competitive.district = 1,
marginality.06 = 0,
PAN.governor.06 = 1
)
predict(poisson_reg, newdata = predict_case, type = "response")
